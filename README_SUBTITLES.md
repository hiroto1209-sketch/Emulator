# Simple Japanese subtitles

This private app keeps the ROM, extracted dialogue index, save-state buffers, and translation cache on the device. The player automatically enables the existing live matcher and shows only the Japanese translation as a compact overlay. The game menu exposes a single `日本語字幕` ON/OFF control instead of the developer-oriented English Study panel.
